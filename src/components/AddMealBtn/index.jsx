import React from 'react';
import { Button } from 'antd-mobile';
import './index.css'
const AddMealBtn = () => {
    return (
        <div >
            <Button className='btn' type="primary" size="large" >添加</Button>
        </div>

    );
}

export default AddMealBtn;

